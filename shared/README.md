# Compass RAG Shared Module

Shared domain schemas (`shared.models.common`), strict configuration (`shared.config`), and structured JSON logging utilities (`shared.logging`) across all Compass RAG microservices.
